import React from 'react';
import ZoomImagesPresenter from './ZoomImagesPresenter';

const ZoomImageContainer = ({ images }) => {
  return <ZoomImagesPresenter images={images} />;
};

export default ZoomImageContainer;
